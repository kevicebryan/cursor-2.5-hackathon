import { Stack, Text, Title } from "@mantine/core";

export default function CollectionPage() {
  return (
    <Stack gap="sm">
      <Title order={2}>Collection</Title>
      <Text c="dimmed">Artifact gallery — coming in Step 8.</Text>
    </Stack>
  );
}
